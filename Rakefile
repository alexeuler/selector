require "rake"
require 'active_record'
require_relative 'lib/config/config'
require_relative 'lib/config/helpers'
dir=File.expand_path(File.dirname(__FILE__))
Dir["#{dir}/lib/tasks/*.rake"].each {|file| load file}